import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class RequestService {

  constructor() { }

  /* TODO Setup methods for all HTTP-Requests to provide them as a service. */
}
